"""unittests.search - unit tests for the GEP search itself.

One module per module under test, plus support.py for the sqlite fixtures the
four database-backed steps need. Run them from the repo root:

    python -m unittest discover -s unittests -t .
"""
